name: Main workflow
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Add path
      run: |
        export PATH="$HOME/.cask/bin:$PATH"
        echo $PATH
    - name: Echo path
      run: |
        echo $PATH
